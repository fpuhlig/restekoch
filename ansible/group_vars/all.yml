ghcr_owner: fpuhlig

redis_host: "{{ vault_redis_host }}"
redis_port: 6379

backend_image: "ghcr.io/{{ ghcr_owner }}/restekoch-backend:latest"
frontend_image: "ghcr.io/{{ ghcr_owner }}/restekoch-frontend:latest"

grafana_password: "{{ vault_grafana_password }}"
